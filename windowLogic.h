#ifndef SIMULATOR_WINDOWACTIONS_H
#define SIMULATOR_WINDOWACTIONS_H

#include "settings.h"

/**
 * Funkcja uruchamiająca aplikację w oknie i zarządzająca pętlą odświeżeń
 */
void createAndRunWindow(SimulationSettings settings);

#endif
